{
  "name": "bali-discount-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-cloudflare.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "nooxy": "2.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
